dist
coverage
node_modules
package-lock.json
playwright-report
test-results

# The resume pages live in public/ with their own vocabulary; never touch them.
public/

# Stylesheets are hand-formatted and linted by stylelint instead.
**/*.css
